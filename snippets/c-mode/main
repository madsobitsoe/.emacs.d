# -*- mode: snippet -*-
# name: main
# key: main
# --
#include <stdio.h>
#include <stdlib.h>


int main(int argc, char* argv[])
{
  if (argc < 2) {
    printf ("Usage: %s ARG1. Exiting!", argv[0]);
    exit(EXIT_FAILURE);
  }

  $0  
  exit(EXIT_SUCCESS);
}
